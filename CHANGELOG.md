## v1.6.0（2026-09-26）

- 习惯：emoji 图标升级为 Material Icons 单色图标库（内置 100+ 个），新建/编辑习惯时图标显示在标题左侧，点击后底部弹出图标选择界面
- 习惯：移除描述（description）字段，习惯仅保留「图标 + 标题」；habit_index 表删除 description 列（schema 8→9 表重建迁移，兼容旧 SQLite；备份/恢复兼容老备份）
- 界面：设置弹窗（智能分类/起始页/外观风格/触感/备份等）内卡片宽度与设置主页卡片对齐（统一 12dp 边距）
- 设置：时间格式默认改为 24 小时制（已存设置不受影响）
- 设置：圆角大小与震动强度两个调节条统一为圆点滑块样式，拖动过程带轻震动反馈
- 设置：智能分类默认勾选改为「今天 / 已过期 / 收集箱 / 已完成」（其余默认隐藏）

## v1.5.9（2026-09-26）

- 界面：移除新建/编辑习惯、新建/编辑分类、重复设置弹窗左上角的圆形装饰图标
- 习惯：卡片左侧改为内置简约 emoji 图标，点击图标打卡完成（再点取消），点击卡片其他区域打开编辑详情
- 习惯：创建/编辑习惯时可从 20 个内置简约 emoji 中选择图标（默认✨）
- 数据：habit_index 表新增 emoji 列（schema 7→8 自动迁移，旧数据默认✨；备份/恢复兼容老备份）

## v1.5.8（2026-09-26）

- 界面：任务/习惯页右下角加号统一为 48dp，与首页一致
- 界面：设置页「外观与风格 / 触感反馈 / 备份与同步」改为从底部弹出弹窗进入（风格与其他弹窗一致），移除独立二级页面
- 重构：三个设置二级页拆出共用 Content（弹窗与独立页同源，改一处全局同步）

## v1.5.7（2026-09-26）

- 重构：恢复备份"引用完整性预校验"抽为纯函数（shared/core RestoreIntegrity，悬空 categoryId/habitId → 拒绝恢复不写库）
- 测试：RestoreIntegrityTest 新增 4 例（悬空分类/悬空习惯/未分类合法/全通过）
- 界面：设置首页「外观与风格 / 备份与同步 / 开源许可」卡片移除左侧图标，与其他卡片一致

## v1.5.6（2026-09-26）

- 重构：任务拖动键计算从 ViewModel 抽为纯函数（TaskReorder：原地释放判断 / 重复任务键 / 普通任务键）
- 修复：原地释放判断改用未排除被拖任务的显示链计算原位置（原实现排除后索引恒为 -1 导致判断失效）
- 测试：TaskReorderTest 新增 10 例（原地释放×3、重复拖顶/拖底/向上插/off-by-one 向下插/相邻重编号、普通拖顶/拖底），回归第六轮 off-by-one 场景

## v1.5.5（2026-09-26）

- 任务卡：点击/长按多选覆盖整卡（标题、副标题、卡片空白区），不再仅标题列触发；复选框保持独立勾选
- 测试：TaskSortingTest 新增 5 例（普通任务拖动落位×2、默认创建倒序、重复任务多拖过混合插入、全部拖过按排序键排列）

## v1.5.4（2026-09-26）

修复：
- 恢复备份：预校验失败返回 Failure 的回归修复（上一版括号错位导致块值丢弃、仍误报成功）
- 深链：冷启动统计/分析页丢失（数据未加载时不再提前清标记，等待数据到达后再入栈）
- 打卡：弹窗快速切换日期及时取消旧查询（ensureActive）
- 习惯：周对比图随周起始设置即时刷新（firstDayOfWeek 纳入统计流 combine）
- 同步：WebDAV 密码解密失败返回空串，不再把密文当凭据发送/显示
- 习惯分析：日期 ticker 单次快照，避免跨午夜瞬间跳一天

清理：
- @Stable+@Immutable 双注解冗余清理（保留 @Stable）
- 无障碍：首页/任务列表 10 处图标补齐 contentDescription
- startedDaysAgo 死字段删除

## v1.5.3（2026-09-26）

A 批（恢复与排序修复）：
- 恢复：Android 10~13 上"恢复备份即崩溃"修复（cancelAll 仅在高版本系统调用 API34+ 方法，低版本逐个取消兜底）
- 恢复：预校验失败不再误报"恢复成功"（withContext 块值正确返回 Failure）
- 排序：重复任务向下拖动 off-by-one 修复（链坐标与排序器"未拖链索引"对齐）

B 批（数据正确性）：
- 恢复：版本门下限提到 UTC 语义起点（v1.4.16，task 11/habit 7），更早备份明确拒绝而非静默偏移
- 统计：日历/热力图 start 早于今天时不再崩溃（4 处 clamp）
- 重复：Weekly 规则加迭代上限守卫，极大间隔/异常配置不再死循环
- 打卡：通知与 ViewModel 统一"星期∈计划日且创建日≤日期≤今天"校验，跨午夜点击不再污染非计划日

C 批（行为一致）：
- 统计：周起始偏好写入任务统计日历表头与偏移（与习惯口径一致）
- 导航：任务统计页系统返回键清除统计状态（与返回箭头一致）
- 拖动：分类拖动延迟移出互斥锁，不再阻塞后续动作；导出/恢复完成后可再次触发
- 编辑：任务日期/时间选择器重开重置；通知权限被拒后授权返回自动续提醒；标题超 100 字提示
- 同步：WebDAV 配置失败消息现在渲染到界面；上传地址用本次输入值

D 批（加固与清理）：
- 清理：TaskRepo 6 个无调用方法（接口+实现+DAO）删除；通知管理器 3 个未用导入删除
- 加固：深色主题启动不再白闪（values-night 覆盖）；应用更新后提醒自动重排（MY_PACKAGE_REPLACED）
- 恢复：CancellationException 正确向上传播，不再误报"恢复失败"；WebDAV 上传补齐 backupFormatVersion 字段；密码解密失败不再把密文当凭据显示
- 习惯：周起始偏好变化即时刷新周图分桶；打卡弹窗快速切换日期及时取消旧查询；startedDaysAgo 死字段删除；8 个月窗口周数修正（32→35 周）；宽屏习惯分析返回栈不再无限增长
- 首页：已过期 tab 副标题显示过期计数（不再误显示今日完成数）；任务拖动原地释放不再写排序键（避免"钉住"当日顺序）
- 测试：Recurrence 新增 3 例（from<base 提前、大间隔、极大间隔兜底），全部通过；全项目 spotless 格式化合规

## v1.5.2（2026-09-26）

- 兼容：备份版本门放宽（迁移链覆盖范围内均可恢复，不再要求版本完全一致）；新增备份格式语义版本字段——无标记的 v1.4.16+ 备份按当前 UTC 语义原样恢复，老版本备份升级后不再"版本不一致"被拒
- 性能：习惯页分钟级空转定时器改为"对齐次日 00:00 触发一次"（跨午夜刷新行为不变，后台不再每分钟重算）
- 清理：删除无调用死代码（getHabitsWithStatus、getHabitsPage、getDeletedTasks、resetAppTheme 接口与实现）；热力图 count==0 永不出现的死分支；TaskList 未用导入；恢复端 Json 实例共享

## v1.5.1（2026-09-26）

C 批（行为一致）：
- 习惯：非计划日（未打卡）的日期格子不再可误点取消；今日/有效日可正常打卡取消
- 同步：WebDAV 上传/下载/保存配置三路失败提示拆开，不再互相串扰；上传/下载直接使用输入框当前值，保存配置失败也给出提示
- 首页：新增"已过期" tab 后，手动滑动不再错位（滑动位置与任务/习惯逻辑 tab 同步）
- 深链：习惯卡统计按钮跳转精确到对应习惯（不再固定打开总体统计），冷启动等待数据就绪后再入栈
- 习惯：打卡/取消动作加互斥锁，快速连点不丢数据
- 启动：数据（生物锁偏好/起始页偏好）就绪前不渲染主界面，起始页后置纠正，避免冷启动白屏/错误页面

D 批（加固与清理）：
- 提醒：点通知主体直接打开 App；通知 ID 改位段防撞（任务低段/习惯高段，id 增长不互相覆盖）；开机/时区/改时间后提醒重排；Manifest 移除多余 USE_EXACT_ALARM 权限、BootReceiver 不再对外导出
- 加固：数据库迁移测试全链覆盖到 v12；重复规则 Yearly 超大间隔/未对齐兜底防溢出，Weekly 负周差改向下取整；本地日期存档非法值不再崩溃；导出取消不再吞异常、OOM 明确报错
- 清理：删除死代码（TaskState.completedTasks、OverallAnalytics.completedHabits、WebDavImpl 多余 Json、ThemeDatastore 废弃键）；分类 chip 改按 id 选中（重命名不失选）；新分类排序位接续当前最大；统计页系统返回键与返回箭头行为一致；首页过期列表过滤结果缓存

## v1.5.0（2026-09-26）

- 安全：恢复备份前增加引用完整性预校验（分类/习惯 ID 悬空直接拒绝，不写任何库，避免"新习惯+旧任务"半恢复）
- 安全：恢复中途异常不再吞掉——提示"数据可能部分更新"，并给出可操作的失败文案（WebDAV 同步恢复同样生效）
- 修复：WebDAV 密码含冒号的旧明文不再被误判为密文而静默清空；密文解密失败时保留原值
- 修复：取消全部提醒改用系统级 cancelAll（进程被杀后残留闹钟也能清干净）
- 说明：备份版本门维持现状（开发阶段不考虑旧数据迁移，恢复老备份仍需手动转换）

## v1.4.21（2026-09-26）

- 修复：Android 10/11/12（API<33）所有习惯/任务提醒静默失效——通知投递判定不再依赖 API 33 才存在的 POST_NOTIFICATIONS 权限，改为低版本直接投递 + 高版本检查系统通知开关
- 修复：点击任务分类后列表不刷新（分类切换与智能清单共用视图重算逻辑）
- 修复：每周热力图表头在"周起始=周日"时错位一天（表头改按实际星期取标签）
- 修复：回收站任务到点弹"幽灵通知"（恢复重建闹钟与通知接收端均过滤软删任务）
- 优化：重复任务拖动改为"拖到哪停哪"（精确落位，次日自动回归频率排序）；新增 TaskSorting 排序单测 7 例

## v1.4.20（2026-09-26）

- 修复：每月重复规则（自定义每 N 月/多选日期）在未对齐起点下死循环导致单元测试卡死——入口对齐编码错位（差 1 个月），对齐后偏移奇偶错位永不命中周期；修复公式并加 2000 个月迭代上限兜底
- 测试：CI 恢复单元测试步骤（`shared:core:testAndroidHostTest`，RecurrenceTest 16/16 通过），测试失败将阻止发版

## v1.4.19（2026-09-26）

- 修复：编辑任务后列表不再显示旧数据（同步判据改为任务内容全等）
- 修复：重复任务区拖动真正生效——当天拖过的按拖位固定，次日自动回归"典型完成时间"频率排序
- 修复：恢复备份失败不再清光已挂提醒（写库成功后才取消旧闹钟并重建调度）
- 修复：首页深链跳习惯统计在冷启动数据未就绪时不再卡死（等待数据就绪后入栈派发，宽窄屏统一）
- 修复：Android 12+ 设备迁移不再泄露 WebDAV 凭据（data_extraction_rules 排除路径修正）
- 修复：习惯页跨午夜自动刷新真正生效（日期信号并入重算链路，三处）
- 修复：导出失败后重试成功，错误红字不再残留；页面退出取消不再误报"导出失败"
- 优化：习惯年/月热力图归一化基准提升到页面级（不再每个格子重复 O(n) 计算）
- 优化：热力图低值颜色下限统一（0.15），无记录格子不再隐形
- 优化：WebDAV 上传改为字符流直写，避免大备份双份内存驻留
- 清理：分类名提交时去首尾空格；任务数兜底统一为数字；热力图废弃 totalHabits 参数删除

## v1.4.18（2026-09-26）

- 无障碍：编辑/展开/返回/新建/删除/关闭等图标补充 contentDescription（读屏可识别）
- 埋点：WebDAV 上传/下载动作补充成功/失败埋点（与其余动作一致）
- 性能：习惯卡打卡日期缓存（重组不重建）、今日完成 ID 转 Set（O(1) 判定）、习惯打卡查询 N+1 → 一次 IN 查询、排序时区一次预取（不再逐元素查系统时区）、典型完成时间改环形中位数（跨午夜不跳变）
- 体验：任务卡提醒时间过期标红；提醒选择器日期时间跟随用户 24 小时偏好、自定义提前量上限 7 天；深链跳统计不再重复入栈（[列表,统计,日历] 场景）；任务多选拖动中数据库回流不再打断拖动
- 导出：导出结果补失败态（Success/取消/失败三态），失败显示原因并可重试，不再静默
- 清理：排序注释 sortIndex → sortKey、折线图"15 周"注释 → 52 周

## v1.4.17（2026-09-26）

- 修复：智能分类"最近7天"范围修正为今天起 7 天（原含今天共 8 天）
- 测试：补充每 N 天重复锚定基准日期的回归用例（RecurrenceTest 13/13 通过）

## v1.4.16（2026-09-26）

- 修复：习惯备份/恢复时间统一 UTC 语义（跨时区设备恢复不再平移习惯提醒时间）
- 修复：WebDAV 上传改分页加载+流式拼接（与本地导出一致，几十万条记录不再全量驻留内存）
- 修复：习惯热力图（总览页）归一化口径统一为历史峰值（加/删习惯不重排历史颜色；习惯数为 0 不再除零）
- 修复：习惯完成率钳制到 0–100%（防御边界场景下超过 100%）

## v1.4.15（2026-09-26）

- 修复：首页跳转习惯统计冷启动竞态（不再卡在"习惯不存在"空页）；习惯统计跨午夜自动刷新；热力图弹窗数据不再被统计流整体覆盖
- 修复：编辑分类/重命名/拖动排序不再强制切换当前分类视图（仅删除当前分类时跳转）；重复任务统计页数据进程恢复后正常回流
- 修复：任务编辑弹窗——过期提醒的存量任务可正常改标题；清除时间后重选日期不再悄悄恢复旧时间；无提醒任务打开提醒选择器不再误勾"到期时提醒"；设置日期不再依赖通知权限
- 修复：提醒选择器/自定义重复等误选；"清空已完成"只清理当前视图可见的已完成任务
- 备份与同步：本地恢复增加二次确认；WebDAV 上传成功提示改用已保存配置；上传/下载取消不再误报失败；连接异常时释放连接；密码解密失败不再把密文发给服务器；保存配置加密异常不崩溃
- 拖动排序：禁止跨"普通/重复任务"分区拖动（避免拖后弹回原位）
- 重复规则：每 N 天重复对齐起始日期（避免周期漂移）

## v1.4.14（2026-09-26）

- 修复：恢复备份 100% 失败（v1.4.13 引入：先插任务后插分类，外键约束导致带分类的任务恢复必失败）——改为分类先插；恢复解码移入后台线程，大备份不再卡界面
- 修复：备份排除规则落空（WebDAV 凭据实际在根目录，规则却排除子目录）——按文件名排除
- 修复：编辑"已完成任务"不再刷新完成时间（避免过期任务编辑后重现、统计被污染、埋点重复计数）；通知栏完成路径补齐完成时间，与界面口径一致
- 修复：习惯统计周起点跨周日错位；习惯星期数据解析容错（单条脏数据不再崩整个习惯列表）
- 修复：删除任务进回收站/彻底删除时取消已挂闹钟（不再到点弹通知、残留幽灵闹钟）
- 稳定性：闹钟调度集合并发加锁，避免多线程崩溃
- 清理：删除生产代码调试输出×2、重复 import

## v1.4.13（2026-09-25）

- 备份与恢复：恢复改为"清空本地 + 写入备份"（各自库内事务保护，中途失败自动回滚，不再残留旧数据）；导出改为分页加载+逐条序列化，几十万条数据不再一次性占满内存
- 性能：习惯数据流共享（首页+习惯页同开时数据库查询与统计只算一遍）
- 习惯统计：分析页与总览页交替切换不再导致返回栈无限增长（多按几次返回即可退出）

## v1.4.12（2026-09-25）

- 通知：习惯/任务通知 ID 空间隔离，互不覆盖；打开弹窗时重新检查通知权限；低版本清除全部提醒改为逐个取消，恢复后不残留旧闹钟
- 稳定性：存档恢复容错（损坏 JSON 不崩溃）；重复任务随机 seriesId 查重防撞号；重复任务拖动排序相邻键冲突自动重编号
- 界面：时间格式时段修正（12 点中午、18 点晚上）；统计/设置界面英文残留全部中文化；习惯统计页/日历页习惯不存在时显示空状态
- 清理：删除冗余 DeleteTask 动作、死参数、重复智能分类标签、重复 import 等死代码
- 工程：CI 移除不可靠的宿主测试步骤（本地已全量验证），新增签名密钥校验

## v1.4.11（2026-09-25）

- 锁竖屏：即使手机横屏也保持竖屏，旋转不再丢失编辑内容
- 恢复备份后任务提醒不再静默失效（自动补调度未过期提醒）
- 重复任务统计页按路由正确打开对应系列（不再串号）
- 重复算法：未对齐的起始日期自动对齐到正确周期，杜绝死循环（新增单测）
- 导出与 WebDAV 同步包含已删除任务，换机恢复不再丢数据
- 习惯打卡边界统一：创建日期前不可打卡（月/年日历、热力图、首页卡）
- 习惯连贯性分母改为从创建日起算（漏打后补卡可回到 100%）
- 跨午夜自动刷新：今天/明天/已过期/首页列表不滞留旧日期
- 编辑任务取消选时间不再被强加旧时间
- 统计页日历动态行数，周日首日不再裁掉最后一天
- 年度重复摘要显示"每 N 年"
- 分类编辑弹窗删除/改名后列表实时同步
- 恢复异常不再卡在"恢复中"（回失败态可重试）
- 任务操作串行化，快速勾选/删除不并发写库
- 任务页副标题移除后统一计数口径；首页副标题显示今日完成数

## v1.4.10（2026-09-25）

- 首页习惯卡统计按钮跳转对应习惯的统计页（修复跳转时序竞态）
- FAB 新建按钮尺寸调整为 48dp
- 任务页移除顶部"已完成"计数副标题（计数不准）
- 首页副标题在日期后显示今日已完成数量（如"2026.9.25 周五 · 1 已完成"）
- 清理调试日志

## v1.4.9（2026-09-25）

- 代码审查路线图第 6 批（P3 剩余项全部完成）：
  - 全界面文字资源化（i18n）：通知渠道与"点击完成"按钮、生物识别提示、备份与 WebDAV 全套、触感反馈、外观与风格、首页、任务统计、重复选择器、日历与热力图、周活动图等均改为中英文资源，硬编码中文清零
  - 移除 2024 硬编码起点：习惯年/月日历与热力图按实际数据年份绘制
  - 修复习惯热力图真实 bug：局部变量遮蔽参数导致所有日期可点，修复后仅习惯选定周几且非未来日期可点
  - 日历组件去重：年视图与月视图格子合并为公共实现，月份标题中文化
  - MainApp 两套完全相同路由表合并为一处
  - 构建配置加固：Git 信息缺失时编译不再失败；清理失效混淆规则与重复资源注册
  - 版权头统一：全部源码与 Spotless 规范统一为 Enlpot + GPL v3
  - 迁移测试补强：全链迁移（4→11 / 4→7）改为真实验证；CI 新增核心单元测试步骤
  - 清理注释类问题：分类颜色注释、卡片圆角文件孤儿注释、周活动英文标签



- 持续清理（代码审查路线图「持续」批）：
  - 恢复开源许可合规入口：设置页新增「开源许可」卡片，点击可查看 GPL v3 许可全文
  - 移除从未被界面触发的 WebDAV 状态重置逻辑，保存配置时自动清空上一次的上传/下载结果
  - 彻底删除已被设置页移除的「卡片高度」整条残留代码（设置项、数据存储、状态、订阅）
  - 合并两套完全相同的 Figtree 字体函数，全界面统一使用一套
  - 删除仅被测试引用的重复任务"是否发生"判定器及对应测试（生成器逻辑不受影响，13/13 单测通过）
  - 删除无调用点的序号后缀工具函数
  - 移除无实际作用的 so 符号保留配置（项目中无 jniLibs）
  - 界面状态恢复（如分类管理、新建表单）增加反序列化容错，旧存档不再导致崩溃
  - 提示音播放加锁同步，快速连续触发不再出现并发播放问题

## v1.4.7（2026-09-25）

- 时区口径改造（第 5 批代码审查修复·下）：
  - 时间戳存储改为不依赖设备时区（统一 UTC 语义），换设备、跨时区使用数据不再错位
  - 存量数据自动一次性迁移：已有的提醒时间、完成时间、创建时间按原时区还原后重算，内容不变
  - 任务库 v10→v11、习惯库 v6→v7 迁移经设备端迁移测试验证（6/6 通过）

## v1.4.6（2026-09-25）

- 体验优化（第 5 批代码审查修复·上）：
  - 新建/编辑任务时旋转屏幕或系统回收进程，已填写的表单内容不再丢失（状态可保存）
  - 分类管理拖动排序时，列表不再被数据库回写打断（只初始化一次，拖动更跟手）
  - 重复任务统计页「近 30 天」日历按星期正确对齐，不再与周表头错位
  - 习惯统计页在习惯间切换时，返回栈不再无限堆积（返回行为恢复正常）
- 安全加固：
  - WebDAV 密码改用 Android Keystore 加密存储（旧明文自动迁移为密文）
  - WebDAV「下载恢复」增加二次确认，防止误触覆盖本地全部数据
  - WebDAV 配置输入框回填修正：进入页面不再因异步加载显示空白/覆盖已填内容
  - 系统云备份/换机迁移不再包含 DataStore（WebDAV 凭据不出设备）
  - 备份文件缺少版本字段时明确报"无效备份"，不再被静默当作当前版本恢复

## v1.4.5（2026-09-25）

- 性能优化（第 4 批代码审查修复，为几十万条数据规模做准备）：
  - 任务数据共享热流：数据库变化只重算一遍，多个列表共用同一份结果
  - 重复任务"典型完成时间"预计算一次、所有视图复用，排序不再反复重建
  - 首页「今日任务 / 已过期」列表移入数据层预计算，界面滑动更流畅
  - 去除无效的历史排序字段开销，分类分组算法线性化

## v1.4.4（2026-09-25）

- 数据层加固（第 3 批代码审查修复）：
  - 习惯打卡唯一化：同一习惯同一天只保留一条打卡记录（历史重复数据自动清理），连续天数统计不再被重复打卡干扰
  - 精确闹钟权限被系统撤销时自动降级（提醒不丢、不再崩溃）
  - 开机重启只重排「未完成且有未来提醒」的任务，不再逐条调度全部历史任务
  - 重复任务完成后的系列查重改为按 seriesId 查询，不再全表加载
  - 移除每次进入页面时的全量闹钟重排（改由创建/编辑/恢复时定点调度 + 开机恢复）

## v1.4.3（2026-09-25）

- 修复通知无法关闭的问题：任务完成后取消的是错误的通知 ID，导致旧提醒一直残留（改为按任务取消防置）
- 修复新习惯/零打卡时统计数据出现 NaN（0/0 除零），进度条不再异常
- 优化习惯卡片日历渲染：打卡日期判定由线性扫描改为集合查询，列表滚动更流畅
- 修复平板/大屏模式下内容区域溢出（右侧内容被挤出屏幕）
- 修复备份导出：用户取消保存对话框时不再误报"导出成功"；导出过程异常不再卡死在"导出中"状态
- 修复主题种子色默认值缺透明度位（与存储层默认值对齐）
- 清理仓库中误提交的历史版本 APK 文件（13 个），仓库体积减小

## v1.4.2（2026-09-25）

- 修复重复任务日期生成算法（核心）：多选日期的月度/年度重复规则漏生成实例的问题（如"每月 5 号、25 号"以前每月只出 5 号；"每 2 年"以前年年生成；建档当月后续的配置日/月份被跳过）
- 配置日超出当月天数时顺延到月末（如每月 31 号遇 2 月按 2 月最后一天计）
- 为重复规则增加非法输入防御（异常数据不会导致死循环或崩溃）
- 补齐核心模块单元测试（14 个用例，覆盖重复规则全部场景与回归）
## v1.4.1（2026-09-25）

- 修复桌面图标对勾超出图标边缘的问题（图形缩放至系统遮罩安全区内，尖角完整显示）
- 修复 Android 13+ 主题图标（monochrome）形状丢失问题
- 设置页底部版本号改为自动同步（随构建版本自动更新，不再手动维护）
## v1.4.0（2026-09-25）

- 应用图标焕新：白底蓝青渐变对勾，采用自适应图标（适配系统各种形状），启动画面图标同步更换
- 修复备份恢复丢失问题：导出/恢复补全创建时间与手动排序字段，恢复后保留原始创建时间与排序；兼容旧备份文件
- 移除无用代码与资源（DayDo+ 付费墙、分析埋点、内置更新日志、多语言切换、字体设置、平台冗余文件等），安装包体积进一步减小
## v1.3.0（2026-09-25）

- 任务列表排序全面改造：所有列表初始按创建时间倒序（新任务置顶，编辑不改动创建时间）
- 手动拖动排序仅影响被拖动的任务（相对排序键），新建任务仍按创建时间置顶
- 重复任务按「典型完成时间」动态排序：同系列最近 20 次完成时刻的中位数（完成不足 3 次按创建时间倒序），完成越早排越上，可覆盖手动拖动
- 已完成任务固定按完成时间倒序（后完成在上），不支持拖动
- 已过期任务按到期日期时间升序（越早越上）
- 移除旧的「重新排序」模式入口，排序只在长按多选模式内拖拽
## v1.2.2（2026-09-25）

- 重复任务卡片显示重复图标 + 日期（已过期时日期红色）
## v1.2.1（2026-09-25）

- 已过期任务卡片日期显示为红色
- 删除任务确认弹窗文案改为「确定删除任务？」
## v1.2.0（2026-09-25）

- 新增「已过期」：首页顶部新增已过期 tab（仅存在过期任务时显示），任务页新增已过期智能分类，设置可开关
- 重复任务逻辑调整：任务是否出现在某天按实例日期判定（完成驱动），过期任务不混入今天；完成过期周期后自动补齐今天及缺失周期
- 修复：重复任务补做时按日期查重，不再重复生成同周期实例
- 已过期任务完成后当天仍显示，次日自动消失
## v1.1.10（2026-09-25）

- 备份与同步：上传/下载按钮文字改为「上传」「下载」，卡片副标题压缩为一行
## v1.1.9（2026-09-25）

- 设置主页卡片间距与任务卡统一（2dp，修复常规设置分组残留的 12dp 间距）
## v1.1.8（2026-09-25）

- 备份与同步：导出/恢复/上传/下载按钮统一移至各自卡片主副标题行右侧
## v1.1.7（2026-09-25）

- 备份与同步：导出/恢复/上传/下载按钮移至卡片右侧
- WebDAV「保存配置」按钮移至「WebDAV 服务器」标题行右侧
## v1.1.6（2026-09-25）

- 备份与同步：WebDAV 服务器配置区域整体包裹在圆角矩形卡片内
- 备份与同步页面内所有操作按钮水平居中显示
## v1.1.5（2026-09-25）

- WebDAV 同步整合进「数据备份」页面，统一为「备份与同步」（本地文件备份/恢复 + WebDAV 云备份/恢复）
- 设置页卡片间距统一为任务卡间距（2dp）
## v1.1.4（2026-09-24）

- 修复：取消完成任务不再播放提示音（仅完成时播放）
- 新增：WebDAV 同步（设置页二级页）——填写服务器/账号/密码，一键上传备份、下载恢复
- 备份数据补全内容、日期、时间、重复、系列等全部字段
## v1.1.3（2026-09-24）

- 任务输入框改为两行：第一行标题（大字号，回车直接创建任务），第二行内容（小字号，回车换行）
- 任务新增「内容」字段，随任务保存
## v1.1.2（2026-09-24）

- 统计入口图标更换为简约柱状图风格，与习惯统计图标统一
- 统计入口图标与标题文字垂直居中对齐
## v1.1.1（2026-09-24）

- 默认触感反馈提示音更换为「叮」音效
- 重复任务统计入口图标优化（描边饼图风格）
- 统计入口位置调整至「编辑任务」标题文字右侧
## v1.1.0（2026-09-24）

- 新增：重复待办统计功能
  - 重复任务详情右上角新增饼图入口，进入统计页
  - 统计项：累计完成 / 完成率 / 当前连续 / 最长连续 / 近 30 天完成日历 / 周几分布 / 完成时刻分布
  - 取消勾选完成会自动从统计中扣除
- 数据层：任务记录新增系列 ID（seriesId）与完成时间戳（completedAt），Room 自动迁移至 v7
- 说明：v1.1.0 起新建/完成的重复任务参与统计，历史重复任务首次完成时自动纳入统计
# DayDo 更新日志

本文件记录每个版本的更新内容，每次发布新版本时在此追加最新条目。

## v1.0.9（2026-09-24）

界面与功能优化：

- 设置页底部增加版本号
- 默认起始页面默认值改为「首页」
- 提示音改为内置音效（叮咚 / 叮 / 嘀，默认叮咚，购买确认风双音），修复提示音不生效问题
- 设置页卡片间距统一
- 任务详情弹窗移除左上角图标

## v1.0.8（2026-09-24）

界面优化：

- 移除自动创建的「默认分类」（添加任务弹窗分类选择不再出现）

## v1.0.7（2026-09-24）

界面优化：

- 任务卡片固定为紧凑高度（比原紧凑档再压缩）
- 移除设置中的「卡片高度」选项

## v1.0.6（2026-09-24）

界面优化：

- 首页、任务页、习惯页右下角新建按钮统一为 45x45 纯图标（加号），去掉文字

## v1.0.5（2026-09-24）

界面优化：

- 首页新建按钮改为纯图标（加号），去掉文字并缩小尺寸

## v1.0.4（2026-09-24）

体积优化：

- 移除桌面小组件功能（全部任务 / 习惯概览 / 习惯连续 / 习惯周图）及其依赖
- 移除 35 种非中文翻译资源，仅保留简体中文
- 安装包体积从约 6.6MB 降至约 5.1MB

## v1.0.3（2026-09-24）

体积优化：

- 标题 / 强调字体统一改为 Figtree，移除 Google Sans Flex 可变字体（3.9MB），安装包体积降至约 6.6MB

## v1.0.2（2026-09-24）

体积优化：

- 移除 5 个未使用的内置字体（Inter / Manrope / Montserrat / Outfit / Poppins），保留 Figtree（正文）与 Google Sans Flex（标题），安装包体积从约 9.6MB 降至约 8.9MB

## v1.0.1（2026-09-24）

Bug 修复：

- 修复首页任务卡进入多选状态后，按返回键直接退出应用的问题（现在先退出多选模式）

## v1.0.0（2026-09-24）

首个正式版本，基于 Grit 二次开发并深度定制：

- 全中文界面与日期时间显示（周起始日可配置，12/24 小时制可切换）
- 首页看板：今日任务 + 今日习惯双 tab
- 任务功能：智能分类、任务卡勾选 / 编辑 / 长按多选（全选、拖动排序、删除二次确认）、多选模式返回键优先退出多选
- 任务设置：时间、提醒、重复（每天 / 每周 / 每月 / 每年 / 自定义周期），重复任务显示重复图标，未设时间默认当天全天
- 设置中心重构：全部卡片主副标题 + 圆角矩形；弹窗统一底部弹出；外观与风格、数据备份、触感反馈为二级页面（无底部导航）
- 外观与风格：全局圆角大小（默认 20dp，实时预览）、卡片高度（紧凑 / 常规 / 宽松）、应用主题、Material You、Amoled、调色板风格
- 触感反馈：总开关、震动强度自定义（0-100%）、内置提示音（5 种）
- 默认起始页面可自定义；主界面返回键直接返回桌面
- 移除字体选项、DayDo Plus、暂停通知、重新排序等无关功能
